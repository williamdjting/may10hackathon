/**
 * Backboard API data models and utilities
 */
export var DocumentStatus;
(function (DocumentStatus) {
    DocumentStatus["PENDING"] = "pending";
    DocumentStatus["PROCESSING"] = "processing";
    DocumentStatus["INDEXED"] = "indexed";
    DocumentStatus["FAILED"] = "failed";
})(DocumentStatus || (DocumentStatus = {}));
export var MessageRole;
(function (MessageRole) {
    MessageRole["USER"] = "user";
    MessageRole["ASSISTANT"] = "assistant";
    MessageRole["SYSTEM"] = "system";
    MessageRole["TOOL"] = "tool";
})(MessageRole || (MessageRole = {}));
/**
 * Create an Assistant object from API response
 */
export function createAssistant(data) {
    const tools = data.tools
        ? data.tools.map((tool) => ({
            type: tool.type || 'function',
            function: {
                name: tool.function.name,
                description: tool.function.description,
                parameters: {
                    type: tool.function.parameters.type || 'object',
                    properties: tool.function.parameters.properties || {},
                    required: tool.function.parameters.required || []
                }
            }
        }))
        : null;
    return {
        assistantId: data.assistant_id,
        name: data.name,
        description: data.description,
        systemPrompt: data.system_prompt,
        tools: tools,
        tokK: data.tok_k,
        embedding_provider: data.embedding_provider,
        embedding_model_name: data.embedding_model_name,
        embedding_dims: data.embedding_dims,
        customFactExtractionPrompt: data.custom_fact_extraction_prompt,
        customUpdateMemoryPrompt: data.custom_update_memory_prompt,
        createdAt: new Date(data.created_at)
    };
}
export function createAttachmentInfo(data) {
    return {
        documentId: data.document_id,
        filename: data.filename,
        status: data.status,
        fileSizeBytes: data.file_size_bytes,
        summary: data.summary
    };
}
export function createMessage(data) {
    const attachments = data.attachments ? data.attachments.map((att) => createAttachmentInfo(att)) : null;
    return {
        messageId: data.message_id,
        role: data.role,
        content: data.content,
        createdAt: new Date(data.created_at),
        status: data.status,
        metadata: data.metadata_,
        attachments
    };
}
export function createLatestMessageInfo(data) {
    if (!data)
        return null;
    return {
        metadata: data.metadata_,
        modelProvider: data.model_provider,
        modelName: data.model_name,
        inputTokens: data.input_tokens,
        outputTokens: data.output_tokens,
        totalTokens: data.total_tokens,
        createdAt: data.created_at ? new Date(data.created_at) : null
    };
}
export function createThread(data) {
    const messages = data.messages ? data.messages.map((msg) => createMessage(msg)) : [];
    return {
        threadId: data.thread_id,
        createdAt: new Date(data.created_at),
        messages,
        metadata: data.metadata_
    };
}
export function createDocument(data) {
    return {
        documentId: data.document_id,
        filename: data.filename,
        status: data.status,
        createdAt: new Date(data.created_at),
        statusMessage: data.status_message,
        summary: data.summary,
        updatedAt: data.updated_at ? new Date(data.updated_at) : null,
        fileSizeBytes: data.file_size_bytes,
        totalTokens: data.total_tokens,
        chunkCount: data.chunk_count,
        processingStartedAt: data.processing_started_at ? new Date(data.processing_started_at) : null,
        processingCompletedAt: data.processing_completed_at ? new Date(data.processing_completed_at) : null,
        documentType: data.document_type,
        metadata: data.metadata_
    };
}
export function createToolCallFunction(data) {
    return {
        name: data.name,
        arguments: data.arguments,
        get parsedArguments() {
            try {
                return JSON.parse(this.arguments);
            }
            catch {
                return {};
            }
        }
    };
}
export function createToolCall(data) {
    return {
        id: data.id,
        type: data.type,
        function: createToolCallFunction(data.function)
    };
}
export function createMessageResponse(data) {
    const attachments = data.attachments ? data.attachments.map((att) => createAttachmentInfo(att)) : null;
    const toolCalls = data.tool_calls ? data.tool_calls.map((tc) => createToolCall(tc)) : null;
    const response = {
        message: data.message,
        threadId: data.thread_id,
        assistantId: data.assistant_id,
        content: data.content,
        messageId: data.message_id,
        role: data.role,
        status: data.status,
        toolCalls,
        runId: data.run_id,
        memoryOperationId: data.memory_operation_id,
        retrievedMemories: data.retrieved_memories,
        retrievedFiles: data.retrieved_files,
        reasoning: data.reasoning ?? null,
        modelProvider: data.model_provider,
        modelName: data.model_name,
        inputTokens: data.input_tokens,
        outputTokens: data.output_tokens,
        totalTokens: data.total_tokens,
        createdAt: data.created_at ? new Date(data.created_at) : null,
        attachments,
        timestamp: new Date(data.timestamp),
        toString() {
            return `MessageResponse(
  message='${this.message}',
  threadId='${this.threadId}',
  content='${this.content}',
  messageId='${this.messageId}',
  role='${this.role}',
  status='${this.status}',
  toolCalls=${JSON.stringify(this.toolCalls)},
  runId='${this.runId}',
  retrievedFiles=${JSON.stringify(this.retrievedFiles)},
  modelProvider='${this.modelProvider}',
  modelName='${this.modelName}',
  inputTokens=${this.inputTokens},
  outputTokens=${this.outputTokens},
  totalTokens=${this.totalTokens},
  createdAt='${this.createdAt ? this.createdAt.toISOString() : null}',
  attachments=${JSON.stringify(this.attachments)},
  timestamp='${this.timestamp.toISOString()}'
)`;
        },
        [Symbol.for('nodejs.util.inspect.custom')]() {
            return this.toString();
        }
    };
    return response;
}
export function createContextUsage(data) {
    if (!data)
        return null;
    return {
        percent: data.percent ?? 0,
        usedTokens: data.used_tokens ?? data.usedTokens ?? 0,
        contextLimit: data.context_limit ?? data.contextLimit ?? 0,
        summaryTokens: data.summary_tokens ?? data.summaryTokens ?? 0,
        model: data.model ?? 'unknown'
    };
}
/**
 * Create a ChatMessagesResponse from API response (single message object or { messages, context_usage }).
 */
export function createChatMessagesResponse(data) {
    const ctxRaw = data.context_usage ?? data.contextUsage;
    const contextUsage = createContextUsage(ctxRaw) ?? undefined;
    const messages = Array.isArray(data.messages)
        ? data.messages.map((m) => createMessageResponse(m))
        : [createMessageResponse(data)];
    const lastMsg = messages[messages.length - 1] ?? null;
    return {
        messages,
        contextUsage: contextUsage ?? null,
        get status() { return lastMsg?.status ?? null; },
        get toolCalls() { return lastMsg?.toolCalls ?? null; },
        get runId() { return lastMsg?.runId; },
        get reasoning() { return lastMsg?.reasoning ?? null; },
        get content() { return lastMsg?.content ?? null; },
        get threadId() { return lastMsg?.threadId; },
        get assistantId() { return lastMsg?.assistantId; },
    };
}
/**
 * Create a ToolOutputsResponse object from API response
 */
export function createToolOutputsResponse(data) {
    const toolCalls = data.tool_calls ?
        data.tool_calls.map((tc) => createToolCall(tc)) : null;
    const response = {
        message: data.message,
        threadId: data.thread_id,
        runId: data.run_id,
        content: data.content,
        messageId: data.message_id,
        role: data.role,
        status: data.status,
        toolCalls: toolCalls,
        memoryOperationId: data.memory_operation_id,
        retrievedMemories: data.retrieved_memories,
        retrievedFiles: data.retrieved_files,
        reasoning: data.reasoning ?? null,
        modelProvider: data.model_provider,
        modelName: data.model_name,
        inputTokens: data.input_tokens,
        outputTokens: data.output_tokens,
        totalTokens: data.total_tokens,
        createdAt: data.created_at ? new Date(data.created_at) : null,
        timestamp: new Date(data.timestamp),
        contextUsage: createContextUsage(data.context_usage ?? data.contextUsage) ?? null,
        toString() {
            return `ToolOutputsResponse(
  message='${this.message}',
  threadId='${this.threadId}',
  runId='${this.runId}',
  content='${this.content}',
  messageId='${this.messageId}',
  role='${this.role}',
  status='${this.status}',
  toolCalls=${JSON.stringify(this.toolCalls)},
  retrievedFiles=${JSON.stringify(this.retrievedFiles)},
  modelProvider='${this.modelProvider}',
  modelName='${this.modelName}',
  inputTokens=${this.inputTokens},
  outputTokens=${this.outputTokens},
  totalTokens=${this.totalTokens},
  createdAt='${this.createdAt ? this.createdAt.toISOString() : null}',
  timestamp='${this.timestamp.toISOString()}'
)`;
        },
        [Symbol.for('nodejs.util.inspect.custom')]() {
            return this.toString();
        }
    };
    return response;
}
/**
 * Convert a tool definition to API format
 * Accepts both structured tool objects and plain JSON objects
 */
export function toolToApiFormat(tool) {
    if (tool.type && tool.function && tool.function.name) {
        return tool;
    }
    return {
        type: tool.type || 'function',
        function: {
            name: tool.function.name,
            description: tool.function.description,
            parameters: {
                type: tool.function.parameters.type || 'object',
                properties: tool.function.parameters.properties || {},
                required: tool.function.parameters.required || []
            }
        }
    };
}
export function createMemory(data) {
    return {
        id: data.id,
        content: data.content,
        metadata: data.metadata,
        score: data.score,
        createdAt: data.created_at,
        updatedAt: data.updated_at
    };
}
export function createMemoriesListResponse(data) {
    const memories = data.memories ? data.memories.map((mem) => createMemory(mem)) : [];
    return {
        memories,
        totalCount: data.total_count,
        page: data.page ?? null,
        pageSize: data.page_size ?? null,
        totalPages: data.total_pages ?? null,
    };
}
export function createMemoryStats(data) {
    return {
        totalMemories: data.total_memories || 0,
        lastUpdated: data.last_updated,
        limits: data.limits
    };
}
export function createModel(data) {
    return {
        name: data.name,
        provider: data.provider,
        modelType: data.model_type,
        contextLimit: data.context_limit,
        maxOutputTokens: data.max_output_tokens ?? null,
        supportsTools: data.supports_tools ?? null,
        apiMode: data.api_mode ?? null,
        embeddingDimensions: data.embedding_dimensions ?? null,
        inputCostPer1mTokens: data.input_cost_per_1m_tokens ?? null,
        outputCostPer1mTokens: data.output_cost_per_1m_tokens ?? null,
        lastUpdated: data.last_updated ? new Date(data.last_updated) : null,
    };
}
export function createModelsListResponse(data) {
    const models = data.models ? data.models.map((m) => createModel(m)) : [];
    return {
        models,
        total: data.total,
    };
}
export function createEmbeddingModel(data) {
    return {
        name: data.name,
        provider: data.provider,
        embeddingDimensions: data.embedding_dimensions,
        contextLimit: data.context_limit,
        lastUpdated: data.last_updated ? new Date(data.last_updated) : null,
    };
}
export function createEmbeddingModelsListResponse(data) {
    const models = data.models ? data.models.map((m) => createEmbeddingModel(m)) : [];
    return {
        models,
        total: data.total,
    };
}
export function createProvidersListResponse(data) {
    return {
        providers: data.providers || [],
        total: data.total,
    };
}
export function createMemoryOperationStatus(data) {
    return {
        operationId: data.operation_id,
        status: data.status,
        memoryIds: data.memory_ids ?? null,
        resultCount: data.result_count ?? null,
        createdAt: data.created_at ? new Date(data.created_at) : null,
        updatedAt: data.updated_at ? new Date(data.updated_at) : null,
    };
}
export function createUsageEvent(data) {
    return {
        id: data.id,
        clientId: data.client_id,
        ledgerId: data.ledger_id ?? null,
        operationId: data.operation_id ?? null,
        messageId: data.message_id ?? null,
        modelName: data.model_name ?? null,
        inputTokens: data.input_tokens,
        cachedInputTokens: data.cached_input_tokens,
        outputTokens: data.output_tokens,
        tokenCostUsd: data.token_cost_usd,
        vectorReads: data.vector_reads,
        vectorCostUsd: data.vector_cost_usd,
        memoryWriteCostUsd: data.memory_write_cost_usd ?? 0,
        totalCostUsd: data.total_cost_usd,
        subscriptionCreditsUsed: data.subscription_credits_used ?? 0,
        regularCreditsUsed: data.regular_credits_used ?? 0,
        createdAt: new Date(data.created_at),
    };
}
export function createUsageEventSummary(data) {
    return {
        date: new Date(data.date),
        model: data.model ?? null,
        inputTokens: data.input_tokens,
        outputTokens: data.output_tokens,
        vectorReads: data.vector_reads,
        vectorCostUsd: data.vector_cost_usd,
        memoryWriteCostUsd: data.memory_write_cost_usd ?? 0,
        creditType: data.credit_type,
        amountDeductedUsd: data.amount_deducted_usd,
    };
}
export function createPaginatedUsageResponse(data) {
    return {
        data: (data.data || []).map((e) => createUsageEvent(e)),
        pagination: {
            page: data.pagination.page,
            pageSize: data.pagination.page_size,
            total: data.pagination.total,
            totalPages: data.pagination.total_pages,
        },
    };
}
export function createPaginatedUsageSummaryResponse(data) {
    return {
        data: (data.data || []).map((e) => createUsageEventSummary(e)),
        pagination: {
            page: data.pagination.page,
            pageSize: data.pagination.page_size,
            total: data.pagination.total,
            totalPages: data.pagination.total_pages,
        },
    };
}
//# sourceMappingURL=models.js.map